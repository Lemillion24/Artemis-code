# Copier dans .env.local (ignoré par Git) et renseigner la clé uniquement en local.
OPENROUTER_API_KEY=

# Facultatif : le runner vérifie la gratuité et les appels d’outils avant exécution.
# ARTEMIS_MODEL=cohere/north-mini-code:free
